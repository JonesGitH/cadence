# Single source of truth for the Cadence version.
# build.bat, cadence.iss, and file_version_info.txt all read from here.
__version__    = "1.0.0"
VERSION_TUPLE  = (1, 0, 0, 0)   # (major, minor, patch, build) for Windows VERSIONINFO
